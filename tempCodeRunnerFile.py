
    # con = sqlite3.connect("soccer.db")
    # cursor = con.cursor()
    # text = str(player)
    # print(text.readline(0))
    
    # #con.execute(f"""INSERT INTO player VALUES ({player})""")
    # con.commit()
    # cursor.close()
    # con.close()